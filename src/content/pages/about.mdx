import {
  Card,
  Grid,
  SectionTitle,
  Tag,
  TagList,
} from "../../components/common/UIComponents";

export const aboutPage = {
  intro:
    "I build clean, interactive web experiences with a focus on thoughtful frontend engineering, polished UI, and maintainable product work. If you share similar passions or want to collaborate on a project, I'd love to connect. Feel free to reach out!",
  abilityTitle: "Tech Abilities",
  interestsTitle: "Interests",
};

export const socialLinks = [
  {
    label: "Email",
    href: "mailto:jammiechen0@gmail.com",
    value: "jammiechen0@gmail.com",
  },
  {
    label: "LinkedIn",
    href: null,
    value: "Add your LinkedIn URL",
  },
  {
    label: "GitHub",
    href: null,
    value: "Add your GitHub URL",
  },
];

export const abilityGroups = [
  {
    title: "Programming Languages",
    items: ["TypeScript", "JavaScript", "HTML", "CSS", "SQL"],
  },
  {
    title: "Tech Stack",
    items: ["React", "Tailwind CSS", "Vite", "Node.js", "Three.js", "MDX"],
  },
  {
    title: "Developer Tools",
    items: ["Git", "GitHub", "pnpm", "VS Code", "Chrome DevTools", "Figma"],
  },
];

export const interests = [
  {
    icon: "\u{1F4BB}",
    label: "Software Development",
  },
  {
    icon: "\u{1F512}",
    label: "Cybersecurity",
  },
  {
    icon: "\u{1F916}",
    label: "Machine Learning",
  },
  {
    icon: "\u{1F4B9}",
    label: "Stock",
  },
  {
    icon: "\u{1FA99}",
    label: "Crypto",
  },
];

export const socialLinkCardClassName =
  "flex min-w-[220px] flex-col gap-1 rounded-full bg-bg-secondary px-5 py-3 text-left transition-[transform,box-shadow] duration-300 ease-[cubic-bezier(0.25,0.1,0.25,1)] hover:shadow-card";

<p className="mx-auto mb-10 max-w-[800px] text-center text-[1.2rem] font-normal leading-[1.6] tracking-[-0.015em] text-text-secondary md:text-[1.4rem]">
  {aboutPage.intro}
</p>

<div className="mx-auto mb-16 flex max-w-[820px] flex-wrap justify-center gap-3">
  {socialLinks.map((link) => {
    const content = (
      <>
        <span className="text-[0.75rem] font-semibold uppercase tracking-[0.08em] text-text-muted">
          {link.label}
        </span>
        <span className="text-[0.95rem] font-medium text-text-primary">
          {link.value}
        </span>
      </>
    );

    if (!link.href) {
      return (
        <div
          key={link.label}
          className={`${socialLinkCardClassName} cursor-default opacity-70`}
        >
          {content}
        </div>
      );
    }

    return (
      <a
        key={link.label}
        href={link.href}
        className={`${socialLinkCardClassName} hover:scale-[1.02] hover:no-underline`}
      >
        {content}
      </a>
    );

})}

</div>

<section className="mb-16">
  <SectionTitle className="text-center">{aboutPage.abilityTitle}</SectionTitle>
  <Grid columns={3}>
    {abilityGroups.map((group) => (
      <Card key={group.title}>
        <h3 className="mb-6 text-[1.35rem]">{group.title}</h3>
        <TagList className="!mt-0 !pt-0">
          {group.items.map((item) => (
            <Tag key={item}>{item}</Tag>
          ))}
        </TagList>
      </Card>
    ))}
  </Grid>
</section>

<section>
  <SectionTitle className="text-center">
    {aboutPage.interestsTitle}
  </SectionTitle>
  <Grid columns={3}>
    {interests.map((interest) => (
      <Card
        key={interest.label}
        className="items-center justify-center gap-4 !p-8 text-center"
      >
        <span className="text-[2.5rem]">{interest.icon}</span>
        <span>{interest.label}</span>
      </Card>
    ))}
  </Grid>
</section>
